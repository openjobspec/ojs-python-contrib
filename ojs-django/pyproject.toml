[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "openjobspec-django"
version = "0.5.0"
description = "Django integration for Open Job Spec (OJS) — background job processing standard"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.11"
authors = [{ name = "OpenJobSpec Contributors" }]
classifiers = [
    "Development Status :: 4 - Beta",
    "Framework :: Django",
    "Framework :: Django :: 4.2",
    "Framework :: Django :: 5.0",
    "Framework :: Django :: 5.1",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "License :: OSI Approved :: Apache Software License",
    "Topic :: Software Development :: Libraries",
    "Topic :: System :: Distributed Computing",
    "Typing :: Typed",
]
dependencies = [
    "openjobspec>=0.5.0,<0.6.0",
    "django>=4.2",
]

[project.optional-dependencies]
dev = [
    "cryptography>=42.0",
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-django>=4.8",
    "ruff>=0.4",
    "mypy>=1.10,<2.0",
    "django-stubs>=5.0",
    "pip-audit>=2.7",
]

[project.urls]
Repository = "https://github.com/openjobspec/ojs-python-contrib"
Changelog = "https://github.com/openjobspec/ojs-python-contrib/blob/main/CHANGELOG.md"
Issues = "https://github.com/openjobspec/ojs-python-contrib/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/ojs_django"]

[tool.hatch.build.targets.sdist]
exclude = [
    "/.consumer",
    "/.venv",
    "/build",
    "/dist",
]

[tool.uv.sources]
openjobspec = { path = "../../ojs-python-sdk", editable = false }

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"
DJANGO_SETTINGS_MODULE = "tests.settings"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM", "A", "C4", "DTZ", "T20", "PT", "RUF"]

[tool.mypy]
python_version = "3.11"
strict = true
plugins = ["mypy_django_plugin.main"]

[tool.django-stubs]
django_settings_module = "tests.settings"
